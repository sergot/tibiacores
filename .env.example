# TibiaCores Environment Variables
# Copy this file to .env and fill in your values

# ============================================
# Core Application Settings
# ============================================

# JWT Secret (REQUIRED)
# - Production: MUST be Base64 encoded string
# - Development: Can be plain string
# Generate with: openssl rand -base64 32
JWT_SECRET=your-jwt-secret-here

# Frontend URL (REQUIRED)
# Used for generating links in emails (verification, password reset, etc.)
FRONTEND_URL=http://localhost:5173

# Server Port (OPTIONAL)
# Default: 8080
PORT=8080

# ============================================
# Database Configuration
# ============================================

# PostgreSQL Connection (REQUIRED)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/tibiacores?sslmode=disable

# ============================================
# Email Service - Mailgun (REQUIRED for email features)
# ============================================

# Mailgun Domain (REQUIRED)
# Your verified Mailgun domain
MAILGUN_DOMAIN=mg.yourdomain.com

# Mailgun API Key (REQUIRED)
# Your Mailgun private API key
# CRITICAL: Backend is hardcoded to use Mailgun EU region (mailgun.APIBaseEU)
# Ensure your credentials are for the EU region or you will get authentication errors
MAILGUN_API_KEY=your-mailgun-api-key

# Email From Address (OPTIONAL)
# The "From" address for transactional emails
# Default: noreply@{MAILGUN_DOMAIN}
EMAIL_FROM_ADDRESS=noreply@yourdomain.com

# ============================================
# Newsletter Service - EmailOctopus (REQUIRED for newsletter)
# ============================================

# EmailOctopus API Key (REQUIRED)
# Your EmailOctopus API key for newsletter subscriptions
EMAILOCTOPUS_API_KEY=your-emailoctopus-api-key

# EmailOctopus List ID (REQUIRED)
# The list ID where new subscribers will be added
EMAILOCTOPUS_LIST_ID=your-list-id

# ============================================
# OAuth Providers (OPTIONAL - if using OAuth login)
# ============================================

# Discord OAuth
# Create app at: https://discord.com/developers/applications
DISCORD_CLIENT_ID=your-discord-client-id
DISCORD_CLIENT_SECRET=your-discord-client-secret
DISCORD_REDIRECT_URI=http://localhost:5173/auth/discord/callback

# Google OAuth
# Create app at: https://console.cloud.google.com/apis/credentials
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_REDIRECT_URI=http://localhost:5173/auth/google/callback

# ============================================
# Notes
# ============================================

# 1. JWT_SECRET: In production, generate a secure Base64 string and keep it secret
#    Example: openssl rand -base64 32
#
# 2. Mailgun EU Region: The application is hardcoded to use Mailgun EU.
#    If you have US region credentials, you will need to modify the code.
#    See: backend/services/email.go
#
# 3. OAuth Redirect URIs: Must match exactly in your provider settings
#    For local development: http://localhost:5173
#    For production: https://yourdomain.com
#
# 4. Database URL: For production, use SSL mode and secure credentials
#    Example: postgresql://user:password@host:5432/dbname?sslmode=require
